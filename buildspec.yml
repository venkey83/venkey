YAML
Version: 0.2
phases:
  install:
    runtime-versions:
      awscli: 1.18
pre_build:
  commands:
    - aws rds create-db-snapshot --db-instance-identifier venkey --region ap-southeast-1 --db-snapshot-identifier venkey-1214122021
    - echo "Temp db ready"
    - echo $ENDPOINT
build:
  commands:
    - echo Build started on `date`
    - echo Build started on `date`
    - echo proceed db connection to $ENDPOINT
    - echo proceed db migrate update, DDL proceed here
    - echo proceed application test, CRUD test run here
post_build:
  commands:
    - echo Build completed on `date`
    - echo $DBNAME
